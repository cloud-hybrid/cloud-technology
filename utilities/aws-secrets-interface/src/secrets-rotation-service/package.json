{
    "name": "@cloud-technology/secrets-rotation-service",
    "description": "...",
    "version": "0.0.4",
    "private": false,
    "dependencies": {
        "aws-sdk": "^2.799.0"
    },
    "devDependencies": {
        "jest": "^26.6.3"
    },
    "scripts": {
        "test": "jest",
        "build": "sam build",
        "start": "sam local start-api",
        "deploy": "sam deploy",
        "delete": "aws cloudformation delete-stack --stack-name secrets-rotation-service"
    }
}

